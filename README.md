# OutputScrub 🧹 — sanitize agent outputs before delivery

Strip PII, redact API keys, format for destination. Plugin-style rules.

```bash
python3 outputscrub.py --input agent_output.txt --output safe.txt
```

🧰 **[Tool on Hermtica Marketplace](https://hermtica.com/marketplace)**
